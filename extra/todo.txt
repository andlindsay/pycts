TBI:
	check for missing variants of CSV mime type that must be permitted
		only CSV that works in Word for Mac is "Windows CSV" format
	use password hashes instead of storing them in cleartext
	keep an administrative activity log?
	backup/restore improvements
		"database dump" to xml, so that all info is captured (don't bother with CSV)
		keep just info in that's in the database (simple xml) or make tarball of xml and all study flyers?
	deployment script?
	error checking:
		have mysql send admin an email if it can't connect?
